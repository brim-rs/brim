use { Span } from "span.brim";

pub fn test() {
}

pub trait Display {
    fn test(self) -> string!;
}

impl Display for Span {
    fn test(self) -> string! {
        format("Span({}..{})", self.start, self.end)
    }
}