use { File } from std::fs::file;
use { getStdErr } from std::io::lib;

struct Vec2<T> {
    x: T,
    y: T
}

pub fn main() {
    let v = Vec2 { .x = 6.5, .y = 2.6 };
    let os = @os();
    let file = File { .handle = 23 };

    let val: any = 42;
    let std = getStdErr();
};