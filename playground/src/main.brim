pub fn div(a: i32, b: i32) -> i32!string {
    if b == 0 {
        return @err("Division by zero");
    }

    return @ok(a / b);
};

pub struct Foo {
    x: i32,
    z: i32,
    y: i32,
    a: i32,
};

pub fn main() {
    let x = div(10, 0);
};